<div class="wrapper">
  <div class= "side-bar">
    <%= render "items/side_bar"%>
  </div>

  <div class="main-display">
    <div class="top-display">
      <div class="top-display-title">
        企業 > 一覧表示
      </div>
      <div class="item-indicate-btn">
        <ul>
          <li class="lightblue", id="sort-by-code">企業一覧</li>
          <li id="sort-by-category">企業別店舗表示</li>
        </ul>
      </div>
    </div>

    <div class="lower-display">

      <div class="sort-by-code", id="sort-by-code-display">
        <%if @companies %>
          <% @companies.each do |company|%>
            <div class="company-display-content">
              <%= company.name%>
              <%= link_to "店舗表示", company_shops_path(company.id) %>
              <%= link_to "納期表", company_path(company.id) %>
            </div>
          <% end %>
        <% end %>
      </div>

      <div class="sort-by-category", id="sort-by-category-display">
        <%if @companies %>
          <% @companies.each do |company|%>
            <div class="company-display-content">
              <%= company.name %>
              <div class="shop-display-contents">
                <% @shops.each do |shop|%>
                  <% if shop.company_id == company.id %>
                    <div class="shop-display-content">
                      <%= shop.name %>
                      <%= shop.prefecture.name %>
                      <%= shop.delivery_days %>
                    </div>
                  <% end %>
                <% end %>
              </div>
            </div>
          <% end %>
        <% end %>
      </div>

    </div>
  </div>
</div>